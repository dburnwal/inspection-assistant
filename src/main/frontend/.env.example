# Development (default — leave empty to use Vite proxy to localhost:8080)
VITE_API_BASE=

# Remote access via secure tunnel (e.g. Cloudflare Tunnel)
# VITE_API_BASE=https://your-tunnel-domain.trycloudflare.com
